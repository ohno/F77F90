      program main
      print *, 'do'
      call do()
      print *, 'if'
      call if(0)
      call if(1)
      call if(2)
      call if(3)
      print *, 'while'
      call while()
      end program